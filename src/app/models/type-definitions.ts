export interface IPaticipants { email: string, username: string };
export interface IStoriesSession { id: string };
export interface IBacklogItem { storyId: string };
export interface IUser {
    userName: string;
    email: string;

}
